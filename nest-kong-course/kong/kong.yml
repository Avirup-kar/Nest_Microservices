_format_version: "3.0"

services:
  - name: user-service
    url: http://host.docker.internal:3000

    routes:
      - name: users-route
        paths:
          - /api/users
        strip_path: false